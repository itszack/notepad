import React from 'react';

const AboutPage = () => {
    return (
        <div className="container-fluid text-center text-success">
            <p>This a Notepad built with React, Redux and Firebase.</p>
        </div>
    );
};

export default AboutPage;
